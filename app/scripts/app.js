(function( $, window ){
    window.App = {};
    App = {
        init:function(){
            var ww = $(window).width(),
                wh = $(window).height(),
                renderer = PIXI.autoDetectRenderer()
        }    
    };
    
    App.init();
}(jQuery, this));